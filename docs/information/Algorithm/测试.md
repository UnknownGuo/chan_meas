1.在/mnt/win_data/data_mea/洛阳测试10ms未平均
这个文件夹下，doppler_test.m和read_bin_CIR_GPS.m是读取bin文件的代码
doppler_test_luoyang.docx记录了测量的一些图片。
其中接收数据帧_1.bin是对应测量场景1。（doppler_test_luoyang.docx）

其中接收数据帧_3.bin是对应测量场景2。
测量场景2与测量场景1相比车速更慢：（doppler_test_luoyang.docx）

接收数据帧_20251222142702031.bin是背靠背校准数据（时间较短持续10s）。


在/mnt/win_data/Viavado_doc的channel_air_v4和channel_ground_v4，分别记录了vivado的源代码，阅读各自的PROJECT_INDEX.md为目录，找出对IQ求平均那个程序，告诉我他是怎么求的平均，是去掉首个IQ和最后一个IQ后求平均，还是去掉首个，还是直接平均。
（已解决是直接平均）

现在要做的事：
1.通过接收数据帧_20251222142702031.bin（虽然时间较短，但是有1000帧，足够说明问题）
计算CFO
2.体现CFO的影响，例如直接对15帧IQ直接平均，对前2帧，前3帧，前5帧，前10帧，做平均时。
当采用接收数据帧_20251222142702031.bin主径与噪底之间的动态范围（xxdB是多少），在途中标注。

当采用其中接收数据帧_1.bin或接收数据帧_3.bin时，随机取一个snapshot（取的第几个要记录下来，主径与噪底的动态范围）。

阅读以上内容，首先在/home/guo/project/chan_meas/写对应的代码转译，把matlab代码翻译为python，如果你觉得读bin文件太慢了，就看看用什么加速一下。
边转译，边实现1.通过接收数据帧_20251222142702031.bin（虽然时间较短，但是有1000帧，足够说明问题）计算CFO。
出一个图就是CFO随时间变化，相邻帧和各帧与第一帧的比较。

第二个就是针对2.体现CFO的影响，例如直接对15帧IQ直接平均，对前2帧，前3帧，前5帧，前10帧，做平均时。
当采用接收数据帧_20251222142702031.bin主径与噪底之间的动态范围（xxdB是多少），在图中标注，以便我来对比其效果。
