我现在要你帮我实现一个“信道测量数据离线分析平台”的 HTML UI 原型，注意这不是纯演示页面，而是后续要逐步接入真实功能的工程界面。所以请你按“可扩展、可接真实逻辑、模块化”的标准来写，而不是只做静态好看的 mockup。

目标：
1. 先实现一个单文件或少量文件组成的 HTML 前端界面，方便我本地调试；
2. 界面视觉风格、布局结构、信息密度，参考我给你的界面截图；
3. UI 现在主要用于调试和功能联调，但所有功能入口都必须是真实可接线的，不要做成纯摆设；
4. 每个面板、按钮、图区、文件选择区、状态区、时间控制区，都要预留清晰的 DOM 结构、事件入口和 JS 接口，方便我后续接入 Python/后端/数据文件解析逻辑。

请按以下要求实现：

【一、总体定位】
这是一个面向无线信道测量 / V2V 信道分析的工程化数据分析仪表盘，不是营销页面。
风格要求：
- 专业、冷静、科研/工程风格；
- 强调“控制面板 + 多图联动 + 数据回放 + 状态总览”；
- 适合长时间调试使用，信息密度高但不能乱；
- 默认支持中文界面；
- 字体清晰，优先保证可读性和紧凑布局。

【二、布局结构】
请实现一个典型三栏式分析平台布局：

1. 顶部 Header
- 左侧：平台名称，例如“V2V信道测量数据离线分析平台”
- 右侧：系统状态区，例如：
  - 数据已加载 / 未加载

2. 左侧 Sidebar（固定宽度）
分成三个卡片区块：
- 数据导入
  - 加载 Rx 数据
  - 加载校准数据（可选）
  - 加载 Tx GPS 数据
  - 每一项都包含：
    - 标签
    - 文件路径显示框
    - “选择文件”按钮
    - 加载状态图标
- 分析选项
  - 是否做校准（checkbox）
  - Tx 静止 / Tx 运动（checkbox 或 radio）
  - 场景类型下拉框
  - 是否显示轨迹线
- 回放控制
  - 时间滑块
  - 播放 / 暂停 / 上一帧 / 下一帧
  - 当前帧、当前时间、总时长、帧率等信息

3. 中央 Main Content
分成上下两层：
- 第一层：
  - 大地图面板：显示 GPS 轨迹底图区域
  - 右上角或旁边可以放色条 / 图例
- 第二层：
  三个并排图表面板：
  - CIR 瀑布图 / 时延-功率图
  - DPSD 瀑布图（Doppler-Delay 图）
  - MUSIC / SAGE 参数结果图（多普勒频率-时延）
- 第三层（底部分析区）
  - 左：CIR 时域曲线
  - 中：统计参数表（路径数、最大功率、平均功率、均方时延、RMS 多普勒等）
  - 右：功率分布柱状图

4. 右侧 Overview Panel
- 数据概览列表
- 包含：
  - Rx 数据状态
  - 校准数据状态
  - Tx GPS 数据状态
  - 场景
  - 当前时间
  - Tx-Rx 距离
  - 数据时段
- 每一项要有图标 + 标签 + 值/状态

【三、功能实现要求】
请不要只写静态 HTML，要有“真实工程界面的结构”：

1. 所有按钮、滑块、复选框、下拉框都要有事件绑定；
2. 即使当前没有真实数据，也要提供 mock 数据驱动的 demo 状态；
3. 所有主要区域封装成清晰模块，例如：
- initLayout()
- bindControls()
- loadMockData()
- updateOverview()
- updateMapPanel()
- updateCIRPlot()
- updateDPSDPlot()
- updateMusicPlot()
- updateStatsPanel()
- syncFrame(frameIndex)

4. 每个功能区要预留后续替换接口，例如：
- loadRxData(file)
- loadCalibrationData(file)
- loadTxGpsData(file)
- renderMap(gpsData)
- renderCIR(data, frame)
- renderDoppler(data, frame)
- renderMPCScatter(data, frame)
- computeStatistics(frame)

5. 播放控制逻辑要完整：
- 支持播放、暂停、上一帧、下一帧、拖动时间轴；
- 不需要接真实后端，但前端逻辑要完整；
- 当前帧变化时，地图、图表、右侧状态、底部统计同步更新。

【四、图形和技术要求】
技术上优先使用：
- 原生 HTML + CSS + JavaScript
或
- 如果你认为更合适，也可以使用一个轻量方案，但不要引入过重框架。

建议：
- 地图区域可以先用静态底图占位，叠加轨迹线 canvas/svg；
- 图表可以用 ECharts / Plotly / Canvas 任一适合的库；
- 整体要求本地直接打开或用简单静态服务器即可运行；
- 不依赖后端才能看到界面效果；
- 所有图表先用 mock 数据生成，风格接近科研分析软件，而不是商业 BI 大屏；
- 配色和布局尽量贴近我提供的参考图。

【五、代码组织要求】
请你输出时遵循：
1. 先给我项目文件结构；
2. 再给完整代码；
3. 代码里写清楚哪些地方是“后续接真实业务逻辑”的接口；
4. CSS 要规范，类名清晰，不要混乱；
5. JS 要模块化，避免把所有代码堆在一个大段里；
6. 所有面板、按钮、图表容器要有明确 id/class，便于我后续接 Python 生成的数据；
7. 页面必须响应式，但优先保证 1920x1080 桌面调试体验；
8. 风格尽量接近工程软件界面，而不是普通网页。

【六、视觉细节要求】
请重点还原以下气质：
- 深蓝色顶栏
- 白色卡片式面板
- 左侧控制区有分组折叠感
- 中央是最核心的数据可视化区
- 地图占据视觉中心
- 图表区边框清晰，标题明确
- 右侧状态栏简洁、规整
- 蓝色按钮，绿色状态“已加载”
- 时间控制区有明显的播放交互感
- 整体像“科研测量分析软件”而不是“产品官网”

【七、特别强调】
这不是一次性展示页面，而是后续我要逐步把真实功能都接进去：
- 文件加载要能替换成真实文件解析；
- 图表要能替换成真实测量数据绘制；
- 统计参数要能替换成真实计算结果；
- 播放控制要能驱动真实帧索引；
- 地图轨迹要能替换成真实 GPS 数据；
所以请从“可维护工程 UI 原型”的角度实现。

请直接开始：
- 先给出你理解后的页面结构说明；
- 然后输出完整 HTML/CSS/JS 代码；
- 最后说明后续如何把真实功能逐步接入。